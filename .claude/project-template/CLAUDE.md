# Project: [PROJECT_NAME]

## What This Is
<!-- One sentence. What it does, who it's for. -->

## Tech Stack
<!-- Only list what's non-obvious. Don't explain what React is. -->
- **Runtime:**
- **Framework:**
- **Database:**
- **Hosting:**
- **Key integrations:**

## Commands
```bash
# Dev
# Build
# Test
# Lint
# Type-check
```

## Project Structure
```
# Top-level only. 5-10 key directories.
```

## Environment Variables
```bash
# Required — NEVER commit values
```

## Project-Specific Rules
<!-- The stuff Claude can't infer. API quirks, naming conventions, architectural decisions.
     Example: "Telnyx uses POST not PATCH for updates"
     Example: "FE category is Y not 8 in Compulife"
     Example: "Never touch files in components/ui/" -->

## Mistakes
<!-- After ANY correction, add it here so it never repeats.
     Format: "Do X, not Y" — one line each. -->

## Meta-Rules (How to Update This File)
<!-- These rules govern HOW Claude writes rules for itself. Do not remove. -->
- When told to update CLAUDE.md, write ONE concise line in the Mistakes section.
- Format: "Do X, not Y" or "Always X when Y" — max 15 words.
- Before adding, check if a similar rule already exists. If so, refine it instead.
- Never add generic coding advice Claude already knows. Only project-specific gotchas.
- If Mistakes section exceeds 30 lines, consolidate related rules into groups.
- After adding a rule, confirm: "Added to Mistakes: [the rule]"

## Current Phase
<!-- What's being worked on right now -->

## Recommended Agents
<!-- 3-5 max, matched to this project -->
- planner (always)
